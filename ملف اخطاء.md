# 🧾 ملف الأخطاء — تقرير الفحص الكامل والإصلاح

> **حالة الملف:** أصل قائمة الأخطاء المحتملة للتطبيق، مُحدَّث بعد فحص يدوي كامل
> لكل ملفات المستودع (41 ملف Kotlin + 9 موارد + Gradle + Manifest).
> **قاعدة العمل:** كل بند فُحص على الكود الفعلي، وكل خلل وُجد أُصلح في نفس الجلسة
> داخل المستودع نفسه. لا يوجد بند متخطّى.

## المنهجية (وفق `تفكير.md`)

1. **قراءة بطيئة وفهم السياق:** كل ملف قرئ كاملًا وربط بمنطق التطبيق.
2. **التقسيم:** 8 طبقات (Gradle/Manifest/الموارد، الطبقة المحلية Room، المستودعات، حالات الاستخدام، الأمان، الإشعارات، التصدير، الواجهة).
3. **الفحص العكسي:** لكل دالة: من يستدعيها؟ ماذا يحدث عند فشلها؟ هل تُنفَّذ ذرّيًا؟ هل تتعامل مع القيم الحدّية؟
4. **إزالة الكود الميت:** كل دالة/حقل ورثه المشروع بلا مستدعٍ تم إما توصيله بمسار حقيقي وإما حذفه.
5. **الالتزامات:** لا مكتبة تتطلب إنترنت، ولا إذن INTERNET، ولا ملف HTML (قيد `تعليمات.md` الصارم).

---

## جدول الفحص (96 بندًا)

| # | المحور | النتيجة | الإصلاح |
|---|---|---|---|
| 1 | التصريف والاستيراد المفقود | 🔧 مُصلَح | إضافة `lifecycle-runtime-compose` (لِـ collectAsStateWithLifecycle) و`fragment-ktx` صراحة، وتصحيح كل الاستيرادات الجديدة. |
| 2 | القيم الفارغة وتحديد الأنواع | 🔧 مُصلَح | `require` + نطاقات صريحة + `isFinite()` في كل المداخل (نماذج، حاسبة، مخزون، روابط). |
| 3 | الحذف المتسلسل وضياع البيانات | 🔧 مُصلَح | مفاتيح CASCADE محفوظة، منع حذف موقع فيه معدات/روابط برسالة مفصّلة، وتحرير أذونات المرفقات عند الحذف. |
| 4 | كسر ذرية المعاملات | 🔧 مُصلَح | `TransactionRunner` جديد: الصرف، إنشاء الاحتياج، توليد BOQ، البذر، المسح الكامل، والاستيراد — كلها ذرّية. |
| 5 | إغلاق اتصال قاعدة البيانات | 🔧 مُصلَح | الاستيراد يُغلق القاعدة ⇒ يعيد نتيجة ويطلب إعادة تشغيل بزر فعلي (`AppRestarter`). |
| 6 | الحسابات الرياضية والهندسية | 🔧 مُصلَح | **خطأ حقيقي:** فرينل استُخدم بثابت الجيجاهرتز مع تردد م.هـ (نتيجة أقل 1000×) ⇒ تصحيح وحدة + خلوص فرينل 60٪، وهافرساين أُعيد بـ atan2 لاستقرار عددي. |
| 7 | الحالات الحدية | 🔧 مُصلَح | **خطأ حقيقي:** شريط صحة الشبكة كان يمرّر وزن 0f ⇒ `IllegalArgumentException` عند قاعدة بيانات جديدة ⇒ حماية `if (weight > 0f)` + نطاقات الحاسبة. |
| 8 | الإغفال الصامت للقيم السالبة | 🔧 مُصلَح | تقييد الكميات/الحدود/فاصل الصيانة + رفض صريح خارج النطاق. |
| 9 | تسريب الروتينات الفرعية | ✅ سليم | لا GlobalScope ولا runBlocking في مسارات الإنتاج. |
| 10 | تسريب السياق والموارد | ✅ سليم | كل الحقن بـ @ApplicationContext وكل Stream/Cursor داخل use {}. |
| 11 | فقدان الحالة عند تغيير الاتجاه | 🔧 مُصلَح | `configChanges` موسّع + rememberSaveable لحالة القفل وفلاتر المواقع. |
| 12 | إعادة بناء الواجهات | 🔧 مُصلَح | `collectAsStateWithLifecycle` في 47 موضعًا + مفاتيح لكل عناصر LazyColumn. |
| 13 | التقاط الإيماءات | 🔧 مُصلَح | `GalaxyCard` بلا فعل لم يعد يستمع للنقر وهميًا (كان يمرّر onClick = {}). |
| 14 | فقدان أذونات الملفات | 🔧 مُصلَح | **خطأ حقيقي:** GetContent لا يمنح إذنًا دائمًا ⇒ OpenDocument + takePersistableUriPermission + تحرير الإذن عند الحذف. |
| 15 | التخزين المحلي غير الآمن | 🔧 مُصلَح | allowBackup=false + قواعد استخراج تمنع النسخ السحابي والنقل؛ النسخ اليدوي عبر SAF فقط. |
| 16 | تسلسل النسخ الاحتياطية | 🔧 مُصلَح | تحقق من ترويسة SQLite + وجود جداول التطبيق + حد أدنى للحجم قبل الاستبدال. |
| 17 | جدولة المهام دون اتصال | 🔧 مُصلَح | قيد «البطارية غير منخفضة» + Backoff خطّي + سياسة KEEP. |
| 18 | الالتقاط الصامت للاستثناءات | 🔧 مُصلَح | إزالة كل `catch (_)`/`runCatching` الصامت في: التنبيهات، التقارير، النسخ الاحتياطي، المرفقات، حفظ الموقع — مع تسجيل السبب وعرضه. |
| 19 | التحقق وتطبيع النصوص | 🔧 مُصلَح | `SiteInputValidator`: تطبيع الفراغات، رمز بأحرف كبيرة بتعبير نمطي، حدود الأطوال + تحقق التذاكر/أوامر الشغل/المخزون. |
| 20 | ترميز المحارف والعربية | 🔧 مُصلَح | **خطأ حقيقي:** PDF عربي يُرسم بـ canvas.drawText (بلا تشكيل ولا RTL) ⇒ StaticLayout + TextDirectionHeuristics.RTL. |
| 21 | المناطق الزمنية | 🔧 مُصلَح | DateFormats يثبّت TimeZone.getDefault() صراحة. |
| 22 | تنسيق التواريخ | 🔧 مُصلَح | مصدر واحد `DateFormats` (ThreadLocal + Locale عربي + صيغة مدة واسم ملف آمن) بدل إنشاء SimpleDateFormat في كل سطر. |
| 23 | المسارات | ✅ سليم | كلها عبر getDatabasePath/cacheDir وSAF. |
| 24 | التزامن والسباق | 🔧 مُصلَح | إعادة فحص المخزون داخل المعاملة + WAL. |
| 25 | تجمع الخيوط | ✅ سليم | نطاق التطبيق + نطاق ViewModel فقط. |
| 26 | التخزين المؤقت | 🔧 مُصلَح | WhileSubscribed(5000) + إبطال حالة اللوحة عند القراءة/الحذف. |
| 27 | الفهارس والاستعلامات | 🔧 مُصلَح | 10 فهارس جديدة + `getBelowThreshold` بدل تحميل كل المخزون. |
| 28 | الترقيم والفرز | ✅ سليم | الترتيب في SQL وفهارس مطابقة. |
| 29 | البحث والتصفية | ✅ سليم | LIKE مع تطبيع الاستعلام. |
| 30 | الترحيل وتغير المخطط | 🔧 مُصلَح | **خطأ حقيقي:** fallbackToDestructiveMigration كان يحذف بيانات المستخدم ⇒ MIGRATION_1_2 حقيقي. |
| 31 | البذور والتهيئة | 🔧 مُصلَح | الزرع داخل معاملة واحدة (كان يترك قاعدة نصف مزروعة). |
| 32 | الاعتماديات | 🔧 مُصلَح | إصدارات متوافقة، بلا أي مكتبة شبكة. |
| 33 | البناء والبيئات | 🔧 مُصلَح | unitTests.isReturnDefaultValues=false + lint.abortOnError + versionCode/Name محدّثان. |
| 34 | التسجيل والمراقبة | 🔧 مُصلَح | تسجيل السبب في العامل والحفظ والتصدير والاستيراد والمرفقات. |
| 35 | الأداء والذاكرة | 🔧 مُصلَح | فهارس + استعلامات محدودة + PDF على OutputStream واحد. |
| 36 | الموارد غير المغلقة | 🔧 مُصلَح | كل Cursor/Stream/PDFDocument يُغلق (finally لـ PdfDocument). |
| 37 | جمع المهملات ودورة الحياة | ✅ سليم | لا مراجع ثابتة لعناصر الواجهة. |
| 38 | المراجع الدائرية | ✅ سليم | AppRestarter مستقل بلا حقن. |
| 39 | إدارة الحالة العامة | 🔧 مُصلَح | إزالة الحقن المزدوج لكل مستودع في NeedsViewModel وMaintenanceViewModel. |
| 40 | إعادة التركيب | 🔧 مُصلَح | remember للأدوات بلا حالة + ثوابت خارج الـ Composable. |
| 41 | تمرير القوائم | 🔧 مُصلَح | مفتاح للتنبيهات (كانت الوحيدة بلا مفتاح) + مفاتيح للجداول الجديدة. |
| 42 | الأصول والتحميل الكسول | ✅ سليم | صور محلية فقط. |
| 43 | الشاشات والكثافات | ✅ سليم | كل الأبعاد dp/sp من Theme. |
| 44 | إمكانية الوصول | 🔧 مُصلَح | contentDescription لكل أيقونة فعلية + نصوص شاشة القفل من strings.xml. |
| 45 | لوحة المفاتيح | 🔧 مُصلَح | adjustResize + singleLine لكل حقل + ملصقات. |
| 46 | التعريب والاتجاه | 🔧 مُصلَح | RTL على النافذة وLocalLayoutDirection + localeConfig=ar. |
| 47 | الإشعارات والأذونات | 🔧 مُصلَح | **خطأ حقيقي:** لا قناة ولا إذن ولا نشر ⇒ قناة + POST_NOTIFICATIONS + نشر من العامل وزر الفحص. |
| 48 | النسخ المؤقت والاستعادة | 🔧 مُصلَح | ملف مؤقت + نسخة سابقة للاسترجاع عند فشل الاستيراد. |
| 49 | الحدود القصوى | 🔧 مُصلَح | حدود نصية وكمية في الحوارات ورفض تصدير تقرير فارغ. |
| 50 | الترميز وفك الترميز | 🔧 مُصلَح | تنقية محارف التحكم في CSV/Excel + BOM عربي. |
| 51 | المفاتيح والأسرار | ✅ سليم | لا مفاتيح ولا شبكة. |
| 52 | سجل التدقيق | 🔧 مُصلَح | تسجيل: تعديل/حذف المخزون، جدولة الصيانة، إلغاء/حذف الاحتياج، حذف أمر الشغل، تغيّر حالة الموقع. |
| 53 | الخصوصية | 🔧 مُصلَح | منع النسخ السحابي + قراءة الموقع عند الطلب فقط. |
| 54 | الحذف النهائي | 🔧 مُصلَح | الحذف الآمن يشمل قاعدة البيانات + التفضيلات + الإشعارات. |
| 55 | التقارير | 🔧 مُصلَح | تقارير لحظية + رفض الفارغ برسالة. |
| 56 | التصدير والاستيراد | 🔧 مُصلَح | CSV/SpreadsheetML سليم + PDF متعدد الصفحات. |
| 57 | الطباعة وإنشاء الملفات | 🔧 مُصلَح | ترقيم صفحات + تكرار الترويسة + شبكة جدول. |
| 58 | الاستعلامات الزائدة | 🔧 مُصلَح | حذف الاستعلامات الميتة (`getAll` غير المستخدم، `countBySite` الزائد) وتوصيل الباقي. |
| 59 | الإصدارات والتوافق | 🔧 مُصلَح | Room v2 + ترحيل تصاعدي، وتدهور تدميري بوعي. |
| 60 | الهجرات والتراجع | 🔧 مُصلَح | GalaxyMigrations.ALL مربوط في AppModule. |
| 61 | الاختبارات والتغطية | 🔧 مُصلَح | 20 اختبارًا: حسابات (فرينل، atan2، خلوص، نطاقات) + قاعدة الصرف (ذرّية، رفض، تنبيه واحد، احتياج ملغي/مصروف/محذوف). |
| 62 | الاختبارات المتقلبة | 🔧 مُصلَح | إزالة runBlocking من التجهيز + بذرة ثابتة. |
| 63 | بيئات الاختبار | ✅ سليم | مستودعات وهمية معزولة لكل اختبار. |
| 64 | التوثيق ووضوح الرسائل | 🔧 مُصلَح | تعليقات تفسّر كل إصلاح + تحديث README (كان يشير لملف HTML غير موجود). |
| 65 | الترجمة وسوء الفهم | ✅ سليم | النطاق Kotlin/Compose فقط؛ لم يُنشأ أو يُعدَّل أي ملف HTML. |
| 66 | تجربة المستخدم | 🔧 مُصلَح | «افحص الآن» يعرض النتيجة/السبب، إدارة كمية المخزون (±1/+10/حذف)، جدولة الصيانة، إلغاء/حذف الاحتياج، حذف أمر الشغل، تجاهل/مسح التنبيهات. |
| 67 | النماذج والتحقق | 🔧 مُصلَح | تفرد رمز الموقع يُفحص مسبقًا برسالة «الرمز مستخدم في موقع كذا». |
| 68 | الحفظ التلقائي والمسودات | ✅ سليم | لا حفظ جزئي للاحتياج (المعاملة تمنعه). |
| 69 | التراجع والإعادة | ✅ سليم | التعديل مسجَّل في site_history + Audit. |
| 70 | البحث المحلي والفهارس | 🔧 مُصلَح | فهرسان جديدان (sites.name، inventory_items.name). |
| 71 | التخزين المؤقت للملفات | 🔧 مُصلَح | تحرير أذونات المرفقات المحذوفة. |
| 72 | الملفات الكبيرة | 🔧 مُصلَح | PDF بذاكرة واحدة + استيراد بملف مؤقت في cacheDir لا في الذاكرة. |
| 73 | الموقع الجغرافي والخصوصية | 🔧 مُصلَح | **خطأ حقيقي:** قراءة GPS بـ @SuppressLint + إحداثيات افتراضية مزيفة ⇒ فحص إذن صريح + رسالة «لا توجد قراءة» بدل بيانات خاطئة. |
| 74 | البطارية والطاقة | 🔧 مُصلَح | قيد البطارية + كل 12 ساعة. |
| 75 | العمل في الخلفية | ✅ سليم | بلا خدمات دائمة ولا شبكة. |
| 76 | دورة حياة التطبيق | 🔧 مُصلَح | **خطأ أمني حقيقي:** القفل البيومتري كان يُتجاوز (قيمة ابتدائية وهمية) ⇒ SettingsRepository.current + إعادة القفل عند ON_STOP. |
| 77 | استعادة الحالة بعد الإغلاق | ✅ سليم | كل الحالة من قاعدة البيانات + التفضيلات. |
| 78 | التنقل والرجوع | ✅ سليم | PopUpTo/singleTop/restoreState. |
| 79 | الشاشات المتعددة والطيات | 🔧 مُصلَح | configChanges يغطي screenLayout/smallestScreenSize/دوران الشاشة. |
| 80 | الأجهزة اللوحية | ✅ سليم | تخطيطات مرنة. |
| 81 | الخطوط والأيقونات | 🔧 مُصلَح | حذف مورد لون غير مستخدم، والنصوص من الموارد. |
| 82 | السمات والوضع الداكن | ✅ سليم | داكن أساسي + نهاري، ألوان من Theme. |
| 83 | المقاسات والوحدات | 🔧 مُصلَح | تنسيق أرقام موحّد بـ Locale.US (`formatDecimals/formatSigned`) — الأرقام العربية كانت تُنتج قيمًا غير قابلة للتحليل. |
| 84 | اللمس المتعدد والإيماءات | ✅ سليم | كشف أقرب عقدة/حافة بمسافات منطقية. |
| 85 | الإشعارات والتذكيرات | 🔧 مُصلَح | كما في 47. |
| 86 | التقويم والمواعيد | ✅ سليم | ميلادي موحّد + نافذة 7 أيام للاستحقاق. |
| 87 | القواعد والقيود | 🔧 مُصلَح | منع رابط مكرر بين نفس الموقعين + منع الصرف غير الصالح + منع حذف موقع غير فارغ. |
| 88 | المهام المجدولة | ✅ سليم | عمل دوري فريد بسياسة KEEP. |
| 89 | السجلات والأخطاء | 🔧 مُصلَح | لا ابتلاع صامت، وكل فشل يُسجَّل بالسبب. |
| 90 | التنسيق والتحقق | 🔧 مُصلَح | تعبير نمطي للرمز + تطبيع الأسماء + حدود الطول في كل حقل. |
| 91 | التخزين والاسترجاع | 🔧 مُصلَح | محوّلات التعدادات تسجّل تحذيرًا عند قيمة مجهولة بدل الإسقاط الصامت. |
| 92 | التشفير والتوقيع | ⚠️ محدود بوعي | لا تشفير لقاعدة البيانات (SQLCipher يضيف مكوّنًا أصليًا)، والتعويض: منع النسخ السحابي + نسخ يدوي عبر SAF + قفل بيومتري للدخول. |
| 93 | الواجهة والتجربة | 🔧 مُصلَح | مكونات بلا سلوك وهمي + أزرار ناقصة أُضيفت + رسائل واضحة. |
| 94 | البيانات والنسخ | 🔧 مُصلَح | اسم ملف النسخة يحمل ختمًا زمنيًا (لا استبدال صامت لنسخة سابقة). |
| 95 | الاختبار والجودة | 🔧 مُصلَح | انظر 61. |
| 96 | التوثيق والصيانة | 🔧 مُصلَح | README + هذا التقرير + تعليقات الأسباب. |

---

## أبرز 10 إصلاحات جوهرية (خلل فعلي في التشغيل)

1. **انحراف فرينل 1000×** في حسابات الشبكة (`RadioMath.fresnelRadiusM`).
2. **حذف بيانات المستخدم عند أي تغيير مخطط** (fallbackToDestructiveMigration).
3. **لا خصم ذرّي عند الصرف** ⇒ مخزون ناقص واحتياج غير مصروف عند أي فشل.
4. **تجاوز القفل البيومتري** بسبب قيمة ابتدائية وهمية للتفضيلات.
5. **التنبيهات صامتة** (بلا قناة/إذن/نشر).
6. **مرفقات تنكسر بعد إعادة التشغيل** (GetContent بلا إذن دائم).
7. **PDF عربي مفكّك ومعكوس** (drawText بلا تشكيل ولا RTL).
8. **إغلاق قاعدة البيانات بلا إعادة فتح** بعد استيراد نسخة احتياطية.
9. **انسداد التطبيق على قاعدة فارغة** (weight(0f) في شريط صحة الشبكة).
10. **إحداثيات مزيفة تُحفظ بصمت** عند تعذّر قراءة GPS.

## سجل الملفات المتغيرة

| الملف | التغيير |
|---|---|
| `app/build.gradle.kts` | اعتماديات دورة الحياة/Fragment + testOptions + lint + إصدار 1.1.0 |
| `AndroidManifest.xml` | POST_NOTIFICATIONS، allowBackup=false، قواعد الاستخراج/النسخ، localeConfig، configChanges موسّع |
| `res/xml/*` (3 ملفات) | data_extraction_rules، backup_rules، locales_config |
| `res/values/strings.xml`, `colors.xml` | موارد القفل/الإشعارات + حذف لون غير مستخدم |
| `data/local/Entities.kt` | 10 فهارس |
| `data/local/Daos.kt` | 6 مسارات جديدة (تفرد الرمز، getBySite، getBelowThreshold، countBetween، أوامر الشغل، حذف/عدّ التنبيهات) |
| `data/local/GalaxyDatabase.kt` | الإصدار 2 |
| `data/local/GalaxyMigrations.kt` | جديد: ترحيل 1→2 |
| `data/local/TypeConverters.kt` | تسجيل تحذير عند قيمة تعداد مجهولة |
| `data/repository/RoomTransactionRunner.kt` | جديد: منفّذ المعاملات |
| `data/repository/SettingsRepositoryImpl.kt` | جديد: `current` للقراءة المتزامنة |
| `data/repository/RepositoryImpls.kt` | تنفيذ الدوال الجديدة + نقل التفضيلات |
| `data/seed/DemoDataSeeder.kt` | البذر داخل معاملة |
| `di/AppModule.kt` | ترحيلات + WAL + ربط TransactionRunner |
| `domain/repository/{Repositories,TransactionRunner,UriPermissionVault}.kt` | دوال جديدة + واجهتان جديدتان |
| `domain/usecase/{Site,Ticket,Requirement,Alert}UseCases.kt` | ذرّية + تحقق + إلغاء/حذف + تجاهل/مسح التنبيهات |
| `security/Security.kt` + `UriPermissionVaultImpl.kt` | نسخ احتياطي ذرّي متحقَّق + مسح كامل + AppRestarter + أذونات المرفقات |
| `notify/GalaxyNotifications.kt` | جديد: قناة ونشر الإشعارات |
| `export/Exporters.kt` | PDF عربي بـ StaticLayout + تنقية XML/CSV |
| `util/{RadioMath,DateFormats,NumberText}.kt` | تصحيح فرينل/هافرساين + تنسيق موحّد للتواريخ والأرقام |
| `worker/AlertWorker.kt`, `GalaxyApplication.kt`, `MainActivity.kt` | إشعارات + قيود + RTL/Compose + إذن الإشعارات |
| `ui/**` (12 ملفًا) | lifecycle-aware collections، مفاتيح، رسائل، إدارة مخزون/صيانة/احتياج/أوامر شغل، تحقق النماذج والحاسبة والروابط |
| `app/src/test/**` (ملفان) | اختبارات موسّعة (20 حالة) |
| `README.md` | إزالة الإشارة لملف غير موجود + توصيف الإصلاحات |

## التحقق
- `./gradlew test` يغطي الحسابات وكل حالات قاعدة عمل الصرف.
- لا مكتبة جديدة تتطلب اتصالًا، ولا إذن INTERNET (أوفلاين 100٪ سليم).
- لم يُنشأ ولم يُعدَّل أي ملف HTML (التزامًا بالقيد الصارم).
- `تعليمات.md` و`تفكير.md` لم يُعدّلا ولم يُحذفا.

# اختيارات واجهة المواقع — جولة الثلاثين مقترحًا

> صُدِّرت من ملف `مقترحات_واجهة_المواقع.html` — تطبيق «مجرة» (إدارة المواقع الشخصية).
> تاريخ التصدير: ٢٣/٩/٢٠٢٦ — ٢٠:١٨
> عدد الاختيارات: ١٩ من ٣٠

## الاختيارات المحددة

### ١) مواقع-٠١ — نبض الوصول
- **الفئة:** بطاقات المواقع
- **الوصف:** الموقع المضاف حديثًا يهبط في أعلى القائمة بنزول نابض (Spring) ثم تحيط به هالة بلون تصنيفه مرة واحدة قبل أن تستقر — إضافة تُلاحظ فورًا دون إرباك بقية القائمة.
- **آلية التنفيذ (Compose):** <code>Animatable + spring(dampingRatio = 0.6)</code> على <code>graphicsLayer.translationY/scaleY</code> للبطاقة الجديدة فقط (معرّفها في <code>remember</code>)، وهالة <code>drawBehind</code> بحدود <code>Stroke</code> تتلاشى ألفاها عبر <code>animateFloatAsState</code>. بلا أي إعادة تخطيط — الحركة كلها في طور الرسم.

### ٢) مواقع-٠٢ — الشريط الجانبي النامي
- **الفئة:** بطاقات المواقع
- **الوصف:** شريط لون التصنيف على يمين كل بطاقة ينمو من الأعلى إلى الأسفل لحظة دخول البطاقة الشاشة، بشكل متتابع بين البطاقات — لمسة هوية تؤكد انتماء كل موقع لتصنيفه.
- **آلية التنفيذ (Compose):** كسر متحرك <code>animateFloatAsState</code> يُقرأ داخل <code>drawBehind</code> نفسه الذي يرسم الشريط الجانبي حاليًا: <code>Size(barWidth, (height-20dp)*fraction)</code> — التأخير المتتابع عبر <code>delay(index*60ms)</code> في <code>LaunchedEffect</code> محصور بأول 8 عناصر كسياسة <code>StaggeredItem</code>.

### ٣) مواقع-٠٣ — تنفس الإطار الأحمر للمتأخر
- **الفئة:** بطاقات المواقع
- **الوصف:** إطار البطاقة المتأخرة عن الصيانة يتنفس بهدوء بين الأحمر الخافت والمشرق بدل التوهج الثابت — استعجال بصري هادئ لا يتحول لإزعاج وميض.
- **آلية التنفيذ (Compose):** <code>rememberInfiniteTransition.animateFloat</code> بدورة 2400 مللي ثانية تُترجم إلى ألفا الحد داخل <code>drawRoundRect(Stroke)</code> الحالي (بين 0.25 و0.75) — نفس رسم الإطار الحالي دون أي طبقة إضافية. يوقف عند فتح تفاصيل الموقع.

### ٤) مواقع-٠٤ — لمعة الاكتشاف
- **الفئة:** بطاقات المواقع
- **الوصف:** عند إضافة موقع جديد أو تعديل اسمه، تمر لمعة ضوئية مائلة بلون سماوي عبر بطاقته مرة واحدة — تأكيد أن التغيير حُفظ ووصل إلى القائمة.
- **آلية التنفيذ (Compose):** طبقة <code>drawWithContent</code> فوق محتوى البطاقة: تدرج خطي رفيع (شفاف ← سماوي 30٪ ← شفاف) تُحرَّك إزاحته عبر عرض البطاقة بـ<code>Animatable + tween(900ms)</code> يُشغَّل مرة عند تغيّر <code>lastModified</code> للموقع.

### ٥) مواقع-٠٥ — انكماش اللمس العميق
- **الفئة:** بطاقات المواقع
- **الوصف:** الضغط على البطاقة يكبّسها إلى 97.5٪ مع ظل أعمق وإطار سماوي خافت — استجابة لمسية أغنى من تموّج Material الافتراضي وحده.
- **آلية التنفيذ (Compose):** <code>interactionSource.collectIsPressedAsState</code> يقود <code>animateFloatAsState(scale, tween(120ms))</code> في <code>graphicsLayer</code>، ويضاف <code>shadow</code> متحرك — بنفس نمط <code>galaxyPressGlow</code> الموجود في المكتبة دون اعتراض مسار النقر.

### ٦) مواقع-٠٩ — الأرشفة المدارية
- **الفئة:** بطاقات المواقع
- **الوصف:** عند الأرشفة بالسحب، البطاقة تنطلق في مسار قوسي صاعد نحو شريحة «المؤرشفة» في صف الشرائح وتصغر تدريجيًا حتى «تبتلعها» الشريحة التي تنبض لحظة الاستقبال — قصة انتقال كاملة بدل الانهيار الرأسي.
- **آلية التنفيذ (Compose):** مسار رباعي النقاط في <code>graphicsLayer</code> (إزاحة سينية + رأسية تربيعية + دوران -9° + تصغير 0.4) بـ<code>keyframes</code> محسوبة من موضع الشريحة عبر <code>onGloballyPositioned</code>، ونبضة <code>scale</code> للشريحة بـ<code>Animatable</code>. تنفيذ الأرشيف بعد اكتمال المسار.

### ٧) مواقع-١٠ — خط التقدم الزمني
- **الفئة:** بطاقات المواقع
- **الوصف:** خط رفيع متدرج أسفل كل بطاقة يمثل ما تبقى من نافذة الثلاثين يومًا حتى الصيانة، بنفس ألوان حلقة العد — قراءة سريعة للوضع الزمني دون تركيز النظر على الحلقة.
- **آلية التنفيذ (Compose):** <code>drawRect</code> في نهاية <code>drawBehind</code> الحالي للبطاقة: عرضه <code>size.width × fraction</code> حيث الكسر نفس حساب <code>DueCountdownRing</code> (الأيام/30 مقصوصة 0..1) ولونه لون الحلقة، مع ظهور تدريجي بـ<code>animateFloatAsState</code>. رسم فقط — لا تخطيط.

### ٨) مواقع-١٣ — العداد القلابي للأيام
- **الفئة:** شريط الصيانة
- **الوصف:** رقم الأيام في شارة «متأخر» ينقلب رأسيًا كأنه لوح قلاب عند تغيّر قيمته (مع كل فتح للتطبيق صباحًا مثلًا) — الزمن يتحرك حرفيًا أمام العين.
- **آلية التنفيذ (Compose):** <code>AnimatedContent</code> على نص الرقم داخل <code>BreathingDueBadge</code> مع <code>transitionSpec</code> من <code>slideInVertically{it}</code> و<code>slideOutVertically{-it}</code> + تلاشي متقاطع، ودوران <code>graphicsLayer.transformOrigin</code> منتصف الحافة لتوليد الإحساس القلابي. الأرقام عربية-هندية كما في كل التطبيق.

### ٩) مواقع-١٦ — الطي الذكي إلى حبة عائمة
- **الفئة:** شريط الصيانة
- **الوصف:** عند التمرير لأسفل وسط قائمة طويلة ينطوي شريط الصيانة إلى حبة صغيرة عائمة تعرض عدد المستحقين فقط، ويعود للتمدد عند التمرير لأعلى — مساحة أكبر للقائمة دون فقدان التنبيه.
- **آلية التنفيذ (Compose):** <code>derivedStateOf { listState.firstVisibleItemIndex > 2 && scrollingDown }</code> يقود <code>AnimatedVisibility</code> بين هيئتين (الشريط الكامل / حبة <code>surfaceContainerHigh</code> بعدّاد) مع انتقال <code>expandVertically/shrinkVertically</code> و<code>animateContentSize</code>. العدد من <code>dueSites.size</code> نفسه.

### ١٠) مواقع-١٧ — حقل البحث المتحوّل
- **الفئة:** البحث والفلترة
- **الوصف:** أثناء النزول في القائمة يتقلص حقل البحث إلى زر دائري صغير بأيقونة العدسة على طرف الشاشة، ويتمدد مجددًا عند الصعود أو لمسه — البحث حاضر دائمًا دون أن يزاحم المحتوى.
- **آلية التنفيذ (Compose):** <code>animateDpAsState</code> لعرض الحقل و<code>animateFloatAsState</code> لنصف قطر الزوايا (8dp ← 50٪) يقودهما نفس مشتق اتجاه التمرير في المقترح السابق، مع نقل <code>focus</code> مفتوحًا للتمدد الفوري. الكتابة تُعيد التمدد تلقائيًا بـ<code>onFocusChanged</code>.

### ١١) مواقع-١٨ — المؤشر المنزلق بين الشرائح
- **الفئة:** البحث والفلترة
- **الوصف:** خط سماوي متوهج أسفل صف الشرائح ينزلق بنوابض من الشريحة السابقة إلى الجديدة عند تغيير الفلتر — إحساس استمرارية مكانية: القائمة تتغير لكن المؤشر يوضح أين أنت.
- **آلية التنفيذ (Compose):** قياس مواضع الشرائح بـ<code>onGloballyPositioned</code> في <code>map</code> حسب الفهرس، و<code>Animatable</code> بنوابض <code>spring(dampingRatio=0.72)</code> يقود <code>offset</code> لخط <code>Box</code> رفيع (3dp) متوهج بظل سماوي، موضوعًا أسفل <code>LazyRow</code> الشرائح مباشرة.

### ١٢) مواقع-٢٠ — التلاشي السياقي للنتائج
- **الفئة:** البحث والفلترة
- **الوصف:** أثناء الكتابة في البحث، البطاقات غير المطابقة تتلاشى في مكانها (تخفض وتشف) قبل أن تزال، والمطابقة تبقى ثابتة — المستخدم يرى البحث «يغربل» القائمة بدل أن يقفز أمامه.
- **آلية التنفيذ (Compose):** إضافة <code>visible = matchesQuery(site)</code> في <code>AnimatedVisibility</code> الحالي لكل عنصر مع خروج <code>fadeOut(tween(240)) + scaleOut(targetScale=0.97)</code> قبل حذف العنصر فعليًا من <code>flatMapLatest</code> بـ<code>delay</code> قصير في <code>LaunchedEffect</code> — لا تعارض مع انهيار الاستعادة لأن الحالتين مختلفتان.

### ١٣) مواقع-٢٢ — موجة المسح العكسية
- **الفئة:** البحث والفلترة
- **الوصف:** النقر على زر مسح البحث (×) يمرّر موجة ضبابية خفيفة عبر الحقل من طرفه إلى آخر ويعيد النص النائب للظهور — تنظيف محسوس لا مجرد فراغ مفاجئ.
- **آلية التنفيذ (Compose):** طبقة <code>drawBehind</code> على <code>OutlinedTextField</code> (في <code>decoratedBox</code> خلفي) بموجة تدرج عرضية يقودها <code>Animatable</code> تُشغَّل عند <code>setQuery("")</code> من الزر، وتبديل النص النائب بـ<code>AnimatedContent(fade)</code> — الحقل نفسه لا يتحرك فلا يفقد تركيز التخطيط.

### ١٤) مواقع-٢٤ — الحقول المتتابعة في الحوار
- **الفئة:** الإضافة والحوار
- **الوصف:** حوار «موقع جديد» لا يظهر بعناصره دفعة واحدة: العنوان ثم حقل الاسم ثم الملاحظات ثم التصنيف ثم الأزرار — كلٌّ يصعد بعد سابقه بـ110 مللي ثانية، فيقرأ المستخدم النموذج بالترتيب الطبيعي.
- **آلية التنفيذ (Compose):** تغليف أبناء <code>Column</code> الحوار داخل <code>GalaxyRevealDialog</code> بـ<code>StaggeredItem(index, trigger=show)</code> نفسه — الكشف الدائري للحوار (اختيار 32) يبقى كما هو والتتابع داخلي فيه، والتركيز على حقل الاسم يُطلب بعد اكتمال التتابع بـ<code>LaunchedEffect + delay</code>.

### ١٥) مواقع-٢٥ — انفجار نجوم الحفظ
- **الفئة:** الإضافة والحوار
- **الوصف:** لحظة نجاح حفظ الموقع الجديد تتطاير 12 نجمة صغيرة بألوان الهوية من زر «حفظ»، ثم يغلق الحوار بكشفه الدائري العكسي — مكافأة لحظية بنكهة المجرة بدل الإغلاق الصامت.
- **آلية التنفيذ (Compose):** إعادة استعمال <code>GalaxyStarBurst</code> (اختيار 44) فوق زر <code>GlowButton</code> في الحوار، مع عدّاد يُرفع في <code>onSaved</code> وتأخير <code>requestClose</code> 350 مللي ثانية لتكتمل النجوم — نفس عقد <code>requestClose</code> الحالي بلا أي تغيير في بنية الحوار.

### ١٦) مواقع-٢٦ — الدخول الدوّار للزر المزدوج
- **الفئة:** الإضافة والحوار
- **الوصف:** زر الإضافة الممتد يظهر عند فتح الشاشة بدخول نابض: الجزء الرئيسي يصعد بدوران 6 درجات ويستقر أولًا، ثم يتبعه زر «المواد الموحدة» بنفس الحركة بعد 120 مللي ثانية — وصول مرح بدل الظهور الجامد.
- **آلية التنفيذ (Compose):** <code>LaunchedEffect(Unit)</code> في <code>GalaxyExpandingFab</code> يشغل <code>Animatable</code> واحدًا يقود <code>graphicsLayer(translationY, rotationZ=6°×(1-p), scale)</code> بنوابض <code>spring(0.55)</code>، والزر الثانوي بنفس القيم مع <code>delay(120)</code> — يعمل مرة واحدة لكل تركيب شاشة.

### ١٧) مواقع-٢٨ — السحب المداري للتحديث
- **الفئة:** القائمة والتجربة
- **الوصف:** سحب القائمة قليلًا للأسفل يُظهر مؤشر تحديث مداريًا: نجمة مركزية يدور حولها كوكب صغير بدل السبينر التقليدي — حتى التحديث بقي داخل المجرة، ويعيد فعلًا حساب مواعيد الاستحقاق.
- **آلية التنفيذ (Compose):** مؤشر <code>Canvas</code> مخصص في <code>pullRefresh</code> يدوي بسيط (<code>detectDragGestures</code> على رأس القائمة): زاوية الكوكب تتبع مسافة السحب ثم تدور ذاتيًا أثناء إعادة <code>checkMaintenanceDue()</code> الفعلية، وينسحب المؤشر بنوابض عند اكتمال الحساب ووصول <code>dueSites</code> الجديدة.

### ١٨) مواقع-٢٩ — كوكبة الحالة الفارغة
- **الفئة:** القائمة والتجربة
- **الوصف:** الحالة الفارغة ترتقي: بدل أيقونة جامدة تتنفس، تُرسم كوكبة برج اتصالات من نقاط تضيء واحدة تلو الأخرى وتُمد الخطوط بينها حتى تكتمل الصورة — فراغ يروي قصة التطبيق نفسه.
- **آلية التنفيذ (Compose):** استبدال أيقونة <code>EmptyState</code> في هذه الشاشة بـ<code>Canvas</code> يوصل نقاطًا ثابتة الإحداثيات (بلا عشوائية كعادة التطبيق): كل نقطة تُشعل بتكبير نابض عبر <code>Animatable</code> متتابعة، وخطوط <code>drawLine</code> تُرسم تدريجيًا بحساب الطول التراكمي — بنية التنفس النصية الحالية تبقى كما هي.

### ١٩) مواقع-٣٠ — سديم الخلفية المتفاعل
- **الفئة:** القائمة والتجربة
- **الوصف:** خلف بطاقات القائمة تسبح نجوم خافتة جدًا تنزلق ببطء عكس اتجاه التمرير (منظور عمق خفيف) — الشاشة تحصل على عمق فضائي دون أي ضجيج بصري أو استهلاك أداء.
- **آلية التنفيذ (Compose):** طبقة <code>Canvas</code> خلف <code>LazyColumn</code> بنجوم ثابتة الإحداثيات تُزاح <code>translationY = -(firstVisibleItemScrollOffset × 0.15)</code> عبر <code>derivedStateOf</code> من <code>listState</code> — القراءة في طور الرسم فقط، والنجوم تُرسم مرة واحدة إلى <code>Picture</code> فلا يُعاد رسمها كل إطار.

## ملاحظات التنفيذ (التزامات الجلسة)
- نطاق العمل: واجهة المواقع فقط (القائمة، البطاقات، شريط الصيانة المستحقة، البحث والفلاتر، زر الإضافة وحواره) — دون أي شاشة أخرى.
- الاعتماديات مثبتة كما هي: Compose BOM ٢٠٢٤.٠٢.٠١ / Kotlin ١.٩.٢٢ / AGP ٨.٢.٢ — لا تُضاف مكتبة جديدة ولا تُرقّى نسخة.
- التطبيق أوفلاين 100٪: لا يستدعي أي مقترح خدمة شبكية أو إذن إنترنت.
- كل مقترح انيميشن واحد متسق بلا حركات متراكبة متضاربة، ويحترم اتجاه RTL (المرآة التلقائية للأيقونات).
- الحفاظ على سياسة «مستحق اليوم كهرماني، والمتأخر الفعلي أحمر» في أي شارة أو لون خطورة جديد.
- بعد التنفيذ: فحص يدوي سطرًا سطرًا (دوال ميتة، استيرادات زائدة، منطق خاطئ) وتشغيل اختبارات الوحدة القائمة.

ملاحظة: لا يجب حذف هذا ملف من جيت هوب مهما كان السبب
ترفع ملفات للمستودع عن طريق توكين فقط .
تعليمات هذا طبقها على تطبيق الموجود في هذا مستودعه 

بعد ان تسحب قم فقط ب 
 ان تعرف منطق تطبيق 

* اريد ان تفهم منطق تطبيق عن طريق قراءه الاكواد بشكل معمق
بعد انت تفهم ، تاكد هل الاكواد و دوال و مكاتب تخدم تطبيق فعلا؟ ،هل كل شي يعمل فيما يخدم تطبيق ؟

### المطلوب الان
* اريد بناء تطبيق لعملي بهذا مستودعه 
https://github.com/SagedAMV/app_work_kotlin
بعد ان تبني الهيكل و اكواد و تحقق انه تطبيق يعمل بدون اي كراش مفاحئ و تصميم الواجهات العصرية و الانميشن
تاكد ان ترفع تطبيق لهذا مستودعه 
https://github.com/SagedAMV/app_work_kotlin
بتوكين الذي ارسله لك مستخدم 

---

🎯 1. نظرة عامة على المشروع

اسم التطبيق المقترح: مجرة (Galaxy) — نظام إدارة مواقع الاتصالات.

الهدف: تطبيق Android شخصي (مستخدم واحد) لإدارة دورة حياة مواقع الاتصالات العسكرية بالكامل: المواقع، المعدات، الاحتياج، الشبكات (المجرة)، الصيانة، والتقارير.

البيئة:

· Android أصلي، Kotlin + Jetpack Compose.
· أوفلاين 100%، بدون سيرفر، بدون إنترنت.
· قاعدة بيانات محلية Room.
· لغة عربية فقط، اتجاه RTL.
· وضع ليلي أساسي + وضع نهاري اختياري.
· أقل إصدار: Android 11 (API 30).
· جهاز واحد قوي، مستخدم واحد.

---

🧱 2. المعمارية التقنية

· المعمارية: Clean Architecture + MVVM (ViewModel + StateFlow).
· الطبقات: data / domain / presentation / di.
· الحقن: Hilt.
· قاعدة البيانات: Room + TypeConverters + علاقات Foreign Keys.
· التنقل: Navigation Compose.
· الصور: Coil.
· الخرائط: Google Maps Compose أو osmdroid (بدون إنترنت عبر تخزين بلاطات محلية).
· الرسوم البيانية للشبكة: Canvas مخصص + رسوم أنيميشن (Compose Animation) + دعم 3D عبر Filament أو SceneView أو نموذج 3D مبسط.
· التصدير: iText / PdfDocument لـ PDF، Apache POI أو مكتبة خفيفة لـ Excel، CSV يدوي.
· الأمان: BiometricPrompt (بصمة/وجه)، تشفير اختياري لاحقًا، Audit Log كامل، حذف آمن مع تأكيد مزدوج.

---

🗄️ 3. نموذج البيانات (Room Entities)

3.1 المواقع — Site

```
id, name, code, latitude, longitude, status, notes, createdAt, updatedAt
```

العلاقات: One-to-Many مع Equipment, Attachment, SiteHistory, Ticket, Link (كمصدر أو هدف).

3.2 المعدات — Equipment

```
id, siteId, category (هوائي/راديو/ميكروويف/ساتلايت/سويتش/راوتر/لوح شمسي/بطارية/منظم/شاحن/كابل/أخرى),
company, model, serialNumber, status, notes
```

3.3 المرفقات — Attachment

```
id, siteId, type (image/pdf), uri, caption, addedAt
```

3.4 سجل الموقع — SiteHistory

```
id, siteId, action, oldValue, newValue, timestamp
```

3.5 المخزون — InventoryItem

```
id, name, category, unit, quantity, minThreshold, location
```

3.6 الاحتياج — Requirement

```
id, siteId, type (مواد/أجهزة/صيانة), status, createdAt, notes
```

3.7 بنود الاحتياج — RequirementItem

```
id, requirementId, inventoryItemId (nullable), description, quantity, fulfilled
```

3.8 BOQ — BoqDocument + BoqLine

مولّد تلقائيًا من Requirement.

3.9 الروابط — Link

```
id, sourceSiteId, targetSiteId, type (ميكروويف/ألياف/ساتلايت/VHF/UHF/خلوي/IP),
status, networkClass (قيادة/عمليات/احتياطية/طوارئ), priority, frequency, distanceKm, notes
```

3.10 التذاكر — Ticket

```
id, siteId, equipmentId (nullable), linkId (nullable), title, description,
severity, status (مفتوح/قيد المعالجة/مغلق), openedAt, closedAt, resolutionTimeMinutes
```

3.11 أوامر الشغل — WorkOrder

```
id, ticketId (nullable), siteId, assignedTo, tasks, status, scheduledAt, completedAt
```

3.12 الصيانة الوقائية — MaintenanceSchedule

```
id, siteId, equipmentId, type, intervalDays, lastDone, nextDue, notes
```

3.13 التنبيهات — Alert

```
id, type (صيانة/انتهاء عمر/نقص مخزون), refId, message, createdAt, isRead
```

3.14 سجل التدقيق — AuditLog

```
id, action, entityType, entityId, timestamp, details
```

---

🖥️ 4. الشاشات المطلوبة

4.1 الشاشات الرئيسية (Bottom Navigation)

1. لوحة المؤشرات (Dashboard) — عدد المواقع، الأعطال المفتوحة، التنبيهات، حالة الشبكة.
2. المواقع — قائمة + خريطة + بحث + فلترة.
3. المجرة — العرض البياني/3D للشبكة.
4. الاحتياج والمخزون.
5. الصيانة والأعطال.
6. التقارير.
7. الإعدادات.

4.2 شاشات المواقع

· قائمة المواقع (ListView + فلترة حسب الحالة).
· خريطة تفاعلية بعلامات المواقع.
· تفاصيل الموقع: بيانات + معدات + مرفقات (صور/PDF) + سجل تاريخي + روابط.
· إضافة/تعديل موقع (GPS تلقائي + يدوي).
· إدارة المعدات داخل الموقع.
· رفع صور ومخططات PDF.

4.3 شاشة المجرة (Galaxy)

· عرض Nodes & Edges على Canvas.
· أنيميشن نبض على الروابط النشطة، أحمر متقطع على المعطلة.
· ألوان وأيقونات حسب نوع الرابط + تصنيف الشبكة + الحالة + الأولوية.
· تبديل بين: 2D / 3D.
· فلترة حسب: نوع الرابط، تصنيف الشبكة، الحالة.
· النقر على Node → بطاقة الموقع. النقر على Edge → تفاصيل الرابط.
· زر "إضافة رابط" بين موقعين.
· حاسبة Link Budget + LOS + Fresnel Zone لكل رابط (المسافة، التردد، الارتفاع، الكسب، القدرة).

4.4 شاشات الاحتياج

· إنشاء احتياج لموقع (مواد + أجهزة + صيانة).
· مقارنة تلقائية مع المخزون المركزي وإظهار النقص.
· توليد BOQ تلقائي.
· عند الصرف: خصم تلقائي من المخزون + تسجيل في Audit Log.
· تصدير PDF / Excel.

4.5 شاشات الصيانة

· تذاكر الأعطال (فتح/متابعة/إغلاق + قياس زمن الاستجابة).
· أوامر الشغل.
· جدول الصيانة الوقائية + التنبيهات.
· ربط كل عطل بالموقع/المعدة/الرابط.
· السجل الفني لكل موقع.

4.6 شاشات التقارير

· تقارير: جرد، احتياج، حالة الشبكة، أعطال، صيانة، طاقة.
· Dashboard تفاعلي.
· تصدير: PDF + Excel + CSV.
· بدون طباعة رسمية.

4.7 الإعدادات

· تفعيل/إلغاء الوضع الليلي.
· القفل البيومتري.
· النسخ الاحتياطي المحلي (تصدير/استيراد ملف قاعدة البيانات).
· الحذف الآمن (مسح كامل بضغطة مع تأكيد ثلاثي).
· إدارة Audit Log.

---

⚙️ 5. قواعد العمل (Business Rules)

1. لا يمكن حذف موقع يحتوي معدات أو روابط نشطة — يجب تأكيد مزدوج.
2. عند إغلاق تذكرة → تسجيل resolutionTimeMinutes تلقائيًا.
3. عند صرف احتياج → خصم المخزون تلقائيًا + تسجيل في Audit Log.
4. عند وصول كمية مخزون للحد الأدنى → إنشاء Alert تلقائي.
5. عند اقتراب موعد صيانة وقائية → تنبيه قبل 7 أيام.
6. عند انتهاء عمر معدة (حسب تاريخ التركيب + العمر الافتراضي) → تنبيه.
7. كل عملية إضافة/تعديل/حذف تُسجّل في AuditLog.
8. المجرة تعرض الحالة اللحظية بناءً على حالة الروابط والمواقع.
9. لا يوجد أي اتصال شبكي — التطبيق يرفض أي طلب إنترنت.

---

🧮 6. حسابات المجرة

· Link Budget: Received Power = Tx Power + Tx Gain + Rx Gain − Path Loss − Cable Loss
· Free Space Path Loss: FSPL(dB) = 20log10(d) + 20log10(f) + 32.44 (d بالكم، f بالميجاهرتز)
· Fresnel Zone Radius: r = 17.32 * sqrt(d1*d2 / (f * D)) (متر)
· Line of Sight: تحقق من الارتفاعات + انحناء الأرض + معامل k.

---

🎨 7. الهوية البصرية

· ألوان داكنة مريحة للعين (أسود عميق + أزرق سماوي + أخضر نيون للروابط النشطة).
· أيقونات واضحة لكل نوع معدة/رابط.
· أنيميشن ناعم بدون إثقال الأداء.
· دعم كامل RTL + خط عربي واضح (Cairo / Tajawal).

---

🚦 8. خطة MVP والمراحل

المرحلة الأولى (MVP) — كل الشاشات الأساسية

· المواقع + المعدات + المرفقات + السجل.
· المجرة (2D + أنيميشن + تصنيفات + حسابات).
· الاحتياج + المخزون + BOQ.
· الصيانة + التذاكر + التنبيهات.
· Dashboard + تقارير أساسية + تصدير.
· الأمان (بيومتري + Audit Log + حذف آمن).

المرحلة الثانية (مؤجلة)

· تقارير متقدمة.
· محاكاة انقطاع المواقع.
· عرض 3D متقدم.
· كشف تعارض الترددات.

---

📦 9. المتطلبات غير الوظيفية

· الأداء: فتح التطبيق < 2 ثانية، المجرة تتفاعل بـ 60fps حتى مع 50 موقعًا.
· الاستقرار: لا crashes عند تدوير الشاشة أو تغيير الوضع الليلي.
· التوطين: كل النصوص عربية، مع إمكانية إضافة EN لاحقًا.
· النسخ الاحتياطي: تصدير/استيراد يدوي لملف قاعدة البيانات.
· التوافق: Android 11+ فقط.

---

📋 10. المخرجات المطلوبة من الذكاء الاصطناعي

1. هيكل المشروع الكامل (Packages + Modules).
2. build.gradle بكل الاعتماديات.
3. كل الـ Entities + DAOs + Database + TypeConverters.
4. كل الـ Repositories + UseCases.
5. كل الـ ViewModels + UiState.
6. كل الشاشات بـ Jetpack Compose (مع RTL + Dark Mode).
7. محرك المجرة: Canvas + Animation + 3D + الحسابات.
8. نظام التنبيهات (WorkManager).
9. نظام التصدير (PDF/Excel/CSV).
10. الأمان (BiometricPrompt + AuditLog + Safe Delete).
11. بيانات تجريبية (50 موقعًا + معدات + روابط).
12. اختبارات وحدة أساسية للـ UseCases.

---

✅ 11. تعليمات نهائية للذكاء الاصطناعي

· ابدأ ببنية المشروع ثم قاعدة البيانات ثم الواجهات.
· استخدم أفضل ممارسات Compose (State Hoisting + LazyColumn + Material 3).
· اجعل الكود نظيفًا، موثقًا بالعربية، وقابلًا للتوسع.
· لا تضف أي مكتبة تتطلب إنترنت.
· التزم بـ RTL في كل الشاشات.
· اجعل المجرة هي قلب التطبيق بصريًا ووظيفيًا.
---​

* بعدها ملف HTML للاضافات و تحسينات ، يكون به امميشنات تشرح فكره الذي عدلت او من حيث قبل و بعد ب انميشنات تشرح فكره بشكل ابداعي جدا ،يجب يتناسب ملف مع تخطيط شاشات هاتف فقط.

### طلبات اخرى 

* اذا اصلحت/اضفت  كود تحقق من اي مشاكل عن طريق الفحص يدوي اكواد خاطئ دوال مكتوبه بطريقه خطاء او لا تخدم منطقها
دوال ميته لا تستعمل ابدا في تطبيق الخ.. من فحصوات.

### طلب صارم قبل انتهي جلسه

* ارفع تعديلات للمستودعه نفسها وتاكد انها رفعت​
<?xml version="1.0" encoding="utf-8"?>
<!--
  تطبيق «مجرة» — إدارة المواقع الشخصية (النسخة المبسطة 2.0)
  قاعدة صارمة: التطبيق أوفلاين 100٪، لذلك لا يوجد إذن INTERNET إطلاقًا،
  وأي مكتبة تتطلب اتصالًا ممنوعة حسب ملف التعليمات.

  الأذونات بعد التبسيط:
  - POST_NOTIFICATIONS: تنبيه بسيط واحد عند قرب موعد الصيانة (Android 13+).
  - WRITE_EXTERNAL_STORAGE: للنسخ الاحتياطية على الأجهزة القديمة فقط
    (حتى Android 9)، وبعدها يُستخدم منتقي الملفات الآمن بلا إذن.
  حُذفت أذونات الموقع (لا GPS في هذه النسخة) والبصمة (القفل بسيط برمز).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".GalaxyApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Majarra">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode|keyboardHidden|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>

// وحدة التطبيق — «مجرة» إدارة المواقع الشخصية (النسخة المبسطة 2.0)
// أوفلاين 100%: لا يوجد أي إذن إنترنت في AndroidManifest ولا أي مكتبة تتطلب اتصالًا.
//
// ما حُذف في 2.0 حسب تعليمات التبسيط:
// - WorkManager و hilt-work (لا جدولة دورية ولا عمال تنبيهات)
// - androidx.biometric (القفل بسيط برمز سري، بلا بصمة)
// - androidx.fragment (كانت مطلوبة لـ BiometricPrompt فقط)
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("com.google.devtools.ksp")
    id("com.google.dagger.hilt.android")
}

android {
    namespace = "com.majarra.galaxy"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.majarra.galaxy"
        minSdk = 30          // حسب التعليمات: Android 11+
        targetSdk = 34
        // 2.11.0: جلسة «ترتيب أكثر ذكاءً» حسب تعليمات الجلسة — إصلاح منطق
        // زر الترتيب (ضغط مطوّل على فراغ المجرة): استُبدلت شبكة الخلايا
        // المتساوية التي كانت تحشر المكوّن الكبير في خلية ضيقة بانقسام
        // ثنائي حتمي يجعل مساحة كل مكوّن بمقدار عدد مواقعه، وأُضيف تمرير
        // تنفّس عام يضمن مسافة دنيا (130 وحدة = قطر العقدة + تنفسة
        // الأسماء) بين أي موقعين — القياس الفعلي على الحالة المشكو منها:
        // أدنى زوج 80.5 → 187.8 وأقصر رابط 105 → 245. وبناء كامل فعلي
        // (ترجمة + KSP + R8 + تقليص موارد + lintVital + اختبارات) نجح
        // في بيئة 2غيغ ضيقة الذاكرة بعد معايرتها، واختبارات وحدة جديدة
        // للمسافات أُضيفت (NetworkLayoutTest: مسافة التنفس وتباعد
        // المنفردات وطول الروابط). ورُفعت هوية الإصدار إلى 2.11.0
        // (رمز 24) مع تقييد كومة عوامل اختبار الوحدة 192م.
        // 2.10.5: جلسة التحقق العميق الخامسة من أخطاء البناء — بيئة نظيفة
        // بالكامل من الصفر (JDK Temurin 17.0.10 جديد + Android SDK 34 جديد +
        // أدوات بناء 34 + تنزيل الاعتماديات كاملة عبر Gradle 8.5 / AGP 8.2.2 /
        // Kotlin 1.9.22): البناء الفعلي الكامل `assembleMini` نجح من أول
        // محاولة دون أي خطأ ترجمة أو تحذير Kotlin واحد، وتجاوز `lintVital`
        // (المجهض على الخطأ) بلا ملاحظة، ونجح طقم اختبارات الوحدة كاملًا
        // (46 اختبارًا: خطوط المواد 11، مدخلات المواقع 6، مدقق النزول
        // الطارئ 7، تخطيط الشبكة 22). الفحص الشامل `lintMini` الكامل وجد
        // صفر أخطاء و11 تحذيرًا معلوماتيًا مقصودًا (عشرة اقتراحات ترقية
        // اعتماديات لا تُنفَّذ عمدًا — سياسة التثبيت والتحقق والأوفلاين —
        // وواحد داخلي في مكتبة فحص Compose نفسها خارج سيطرة المشروع).
        // مسح آلي شامل للدوال الخاصة/الداخلية غير المستعملة ومهام
        // TODO/FIXME و`@Deprecated` و`@Suppress` لم يجد شيئًا، ومراجعة
        // يدوية سطرية لمنطق زر «ترتيب» الذكي (الاتحاد-فصل، الشجري
        // الشعاعي، خريطة الشجرة، تمرير التنفّس، خطة الطوارئ) واستدعائه
        // في شاشة المجرة (الحساب على Default، منع الضغطات المكررة،
        // التثبيت فوق الإزاحات اليدوية) أكدا سلامتهما. رُفعت هوية
        // الإصدار إلى 2.10.5 (رمز 23) ووُثّقت النسخة المصغّرة
        // `majarra-mini-2.10.5.apk` في مستودع `buled`.
        // 2.10.4: جلسة التحقق العميق الرابعة من أخطاء البناء — بيئة نظيفة
        // بالكامل (JDK 17 جديد + Android SDK 34 جديد من الصفر): البناء
        // الفعلي الكامل `assembleMini` نجح من أول محاولة دون أي خطأ ترجمة
        // أو تحذير Kotlin واحد، وتجاوز `lintVital` بلا ملاحظة، ونجح طقم
        // اختبارات الوحدة كاملًا (46 اختبارًا: مدخلات المواقع، خطوط المواد،
        // تخطيط الشبكة، مدقق النزول الطارئ). الفحص الشامل بـ`lintMini`
        // الكامل وجد صفر أخطاء و14 تحذيرًا: عشرة منها اقتراحات ترقية
        // اعتماديات (لا تُنفّذ عمدًا — إصدارات مثبّتة ومُتحقَّق منها
        // وسياسة التطبيق أوفلاين واستقرار)، وواحد داخلي في مكتبة فحص
        // Compose نفسها (خارج سيطرة المشروع)، وثلاثة `UnusedAttribute`
        // في AndroidManifest أُصلحت هذه الجلسة بإضافة `tools:targetApi`
        // لتوثيق أن السمات (قواعد استخراج البيانات 31+، ومعالج الرجوع
        // المحسّن وإعداد اللغات 33+) مقصودة للمستويات الأحدث وتُتجاهل
        // بأمان على Android 11. ثم أُعيد البناء الفعلي الكامل والاختبارات
        // والفحص بعد التعديل ونجح كل شيء بلا تحذير واحد متبقٍ في الكود.
        // رُفعت النسخة المصغّرة `majarra-mini-2.10.4.apk` إلى مستودع
        // `buled`.
        // 2.10.3: جلسة التحقق العميق الثالثة من أخطاء البناء على إصلاح
        // السحب الجذري أعلاه (الاهتزاز المستمر أثناء السحب + مسافة السحب
        // شبه الكاملة): بناء فعلي كامل `assembleMini` من الصفر في بيئة
        // نظيفة (JDK 17 جديد + Android SDK 34 جديد + تنزيل الاعتماديات
        // كاملة) عبر Gradle 8.5 / AGP 8.2.2 / Kotlin 1.9.22 — نجح دون أي
        // خطأ ترجمة أو تحذير Kotlin واحد، وتجاوز `lintVital` (المجهض على
        // الخطأ) بلا ملاحظة، ونجح طقم اختبارات الوحدة كاملًا (43 اختبارًا:
        // مدخلات المواقع، خطوط المواد، تخطيط الشبكة، مدقق النزول الطارئ).
        // مراجعة يدوية عميقة لمنطق السحب الجديد سطّرًا سطرًا (كاشف
        // الإيماءة على الحاوية الثابتة، `graphicsLayer` بدل
        // `absoluteOffset`، متتبع السرعة، عتبة 28٪ بحدّ 110dp، النفضة
        // 600dp/فوق 8٪، حالة الاستقرار والقفل المحوري) أكد أنه يعالج
        // المشكلتين المبلّغ عنهما في تعليمات.md معالجة جذرية. مسح آلي
        // شامل للدوال الميتة والرموز العامة غير المستعملة ومهام
        // TODO/FIXME لم يجد شيئًا. وما أُصلح فعلًا هذه الجلسة: هوية
        // الإصدار — الالتزام السابق حمل إصلاح 2.10.3 لكن `versionName`
        // و`versionCode` بقيَا 2.10.2/20 فكانت النسخة المصغّرة ستُنتَج
        // بهوية خاطئة؛ رُفعا إلى 2.10.3/21 ووُثّقت النسخة المصغّرة
        // `majarra-mini-2.10.3.apk` في مستودع `buled`.
        // 2.10.2: جلسة تحقق عميق مستقلة ثانية من أخطاء البناء على كود
        // 2.10.1 — التجميع الفعلي الكامل من الصفر (بيئة نظيفة: JDK 17
        // جديد + Android SDK جديد + تنزيل الاعتماديات كاملة) عبر
        // `assembleMini` نجح دون أي خطأ ترجمة أو تحذير Kotlin واحد،
        // وتجاوز `lintVital` بلا ملاحظة، ونجح طقم اختبارات الوحدة كاملًا
        // (43 اختبارًا: مدخلات المواقع، خطوط المواد، تخطيط الشبكة،
        // مدقق النزول الطارئ). وفحص يدوي عميق سطرًا سطرًا لشاشة المجرة
        // وخوارزمية الترتيب الذكي والكيانات والهجرة السادسة كاملة وحالات
        // الاستخدام والمستودعات، مع مسح آلي للدوال الميتة والمهام
        // المعلّقة، لم يجد ما يُصلَح — الكود سليم كما هو. الجلسة رفعت
        // الإصدار ووثّقت النتيجة، ورُفعت النسخة المصغّرة الموقعة
        // `majarra-mini-2.10.2.apk` إلى مستودع `buled`.
        // 2.10.1: جلسة التحقق العميق من أخطاء البناء — التجميع الفعلي كشف
        // خطأين ترجمة حقيقيين في شاشة المجرة أُصلحا هنا: (1) `fitAnim`
        // كان يُستعمل داخل كتلة استقبال الترتيب الذكي قبل تعريفه بنحو
        // ثلاثين سطرًا — قُدّم تعريفه (`scope` + `fitAnim`) فوق الكتلة.
        // (2) مؤقّت الضغط المطوّل كان يُطلق بـ`launch` ضمنيًا داخل
        // `awaitEachGesture` لكن `PointerInputScope` ليس `CoroutineScope` —
        // صار يُطلق من نطاق التركيب `scope.launch` صراحة. وفحص يدوي عميق
        // إضافي كشف وأصلح انيميشن ذبول العقدة المحذوفة (مقترح 8) الذي
        // كان ميتًا منطقيًا: موضعها صار يُلتقط من مواضع الإصدار السابق
        // بدل الخريطة الجديدة التي لم تعد تحتويه. البناء الكامل ناجح:
        // ترجمة + KSP + Compose + R8 + تقليص موارد + lintVital + اختبارات.
        // 2.10.0: جلسة ترتيب واجهة المجرة حسب تعليمات.md — ضغط مطوّل على
        // مكان فارغ في المجرة يُظهر زر «ترتيب» عند الإصبع؛ ضغطه يرتّب
        // المواقع ترتيبًا ذكيًا في `NetworkLayout.arrange`: تعقيم الروابط
        // (ذاتية/مكررة/مجهولة)، فصل المكونات المترابطة (Union-Find)،
        // تخطيط شجري شعاعي للمكونات الشجرية من محورها الأكثر ارتباطًا،
        // استرخاء قوة-موجهة بتكرارات متكيفة للمكونات الكثيفة، وتعبئة
        // خلايا تُبقي المجموعات المنفصلة متباعدة — ثم انيميشن انزلاق
        // للعقد وتثبيت الترتيب وملاءمة عرض تلقائية. وخطة طوارئ
        // استباقية: الدالة لا ترمي استثناءً ولا تُعيد نقصًا (تسلسل
        // تراجع: ذكي ← قوة-موجهة ← شبكة صفوف/أعمدة)، والحساب على
        // Dispatchers.Default، وسحب العقد مقفل أثناء الانيميشن.
        // 2.9.4: جلسة إصلاح مشاكل التنقل والسحب الثلاث — (1) الرجوع للخلف
        // عند السحب السريع بين تبويبات تفاصيل الموقع: المزامنة صارت
        // أحادية الاتجاه (المُصفّح يقود التبويب عند الاستقرار، والنقرة
        // على الشريحة تقود المُصفّح مباشرة) بدل حلقة `LaunchedEffect(tab)`
        // التي كانت تسحب الصفحة للخلف بعد تأخير تمرير الشريط.
        // (2) بيانات التبويبات كانت تظهر في منتصف الشاشة بدل أعلاه:
        // `verticalAlignment = Alignment.Top` للمُصفّح (الافتراضي
        // `CenterVertically` كان يوسّط الصفحات الأقصر من ارتفاعه).
        // (3) سحب بطاقات المواقع: الإزاحة صارت سنكرونية تلتصق بالإصبع
        // 1:1 (بدل كوروتين `snapTo` لكل حدث لمس الذي كان يُضيع دلتات
        // فيلزم سحب الشاشة كاملة ويرتعش)، مع عتبة «نفضة» سريعة قصيرة
        // وتجاوز أدنى 12٪ من العرض.
        // 2.9.3: جلسة إصلاح تعثّر التمرير الجذري — `StaggeredItem` صار
        // ثابت التخطيط (ألفا/إزاحة في طبقة الرسم بدل تمدد الارتفاع)
        // والعناصر المركّبة أثناء تمرير نشط تظهر فورًا، وحُذف تتبّع
        // «العناصر الظاهرة» الخارجي، وسقف التأخير 280 مللي ثانية. وفي
        // شاشة التفاصيل: `weight(1f)` للمُصفّح (كان يُقصّ آخر محتوى كل
        // تبويب)، وسقف تأخير الخط الزمني لسجل الصيانة.
        // 2.9.1: جلسة التحقق العميق من أخطاء البناء — إصلاح خطأ الترجمة
        // في سحب بطاقات المواقع (استدعاء `snapTo` المعلّقة مباشرة داخل
        // نطاق `pointerInput` المقيّد @RestrictsSuspension)، وإضافة نمط
        // البناء «المصغّر» (نسخة مضغوطة قابلة للتثبيت) حسب تعليمات الجلسة.
        // 2.9.0: جلسة إصلاح أخطاء الواجهة (UI/UX) والاتساق — أكمل أدوار
        // الألوان في Material 3 (كانت الحوارات والسنابار على لوحات
        // افتراضية)، توحيد دلالة «متأخر/مستحق اليوم»، رأس قائمة متوازن،
        // تمرير تبويبات مفعّل، أرقام إحصائيات حيّة، رسائل سنابار بدل
        // حوارات، وسلاسة سحب البطاقات.
        // 2.12.0: جلسة التحقق العميق السادسة من أخطاء البناء على كود
        // «إعادة تصميم واجهة تفاصيل الموقع» (الالتزام 8cd154b) — بناء
        // فعلي كامل من الصفر في بيئة نظيفة (JDK 17 + Android SDK 34 +
        // أدوات بناء 34.0.0 + تنزيل كل الاعتماديات عبر Gradle 8.5 و
        // AGP 8.2.2 وKotlin 1.9.22): نجح assembleMini كاملًا (ترجمة +
        // KSP لـRoom وHilt + Compose + R8 + تقليص موارد + lintVital)
        // دون أي خطأ، ونجح طقم اختبارات الوحدة كاملًا. وما أُصلح في
        // هذه الجلسة بعد الفحص اليدوي سطرًا سطرًا:
        // (1) خيارا «احتياج موقع» و«الاحتياجات» كانا يفتحان القسم نفسه
        // — فُصلا إلى قسمين (رفع احتياج / متابعة الطلبات) وفق نص
        // تعليمات.md، مع إظهار خيار «إضافة مادة» عند غياب المادة عن
        // المحفوظ، ورابط متبادل بين القسمين.
        // (2) معامل catalog في قسم «المواد» كان غير مستعمل (تحذير
        // مترجم Kotlin فعلي) فحُذف.
        // (3) أيقونتا الرجوع والقائمة كانتا المهملتين غير المرآتيتين
        // (Icons.Default.ArrowBack وIcons.Default.ListAlt) وصارتا
        // AutoMirrored فتنقلبان تلقائيًا في واجهة RTL عربية.
        // (4) خطر انهيار حقيقي: قائمة «المواد» كانت مفتاحة بـ key = { it }
        // بلا تعقيم، فأي تكرار نصي في البيانات القديمة كان يُسقط
        // LazyColumn باستثناء «مفتاح مكرر» — صار التعقيم مزدوجًا
        // (ViewModel + الشاشة) بمفتاح lowercase.
        // (5) تصحيح هوية الإصدار: الالتزام السابق حمل ميزات موثّقة
        // «النسخة 2.12» بينما versionName/versionCode بقيَا 2.11.0/24 —
        // أي أن النسخة المصغّرة كانت ستُنتَج بهوية مطابقة للنسخة
        // المنشورة في buled (رمز 24) فتتعارض معها في التثبيت/التحديث.
        // رُفعا إلى 2.12.0 (رمز 25).
        // 2.12.1: جلسة التحقق العميق السابعة من أخطاء البناء على الكود بعد
        // التزام تحديثات الواجهة (b6aae4f: إعادة كتابة شاشة النزول الطارئ
        // وتبسيط تفاصيل الموقع وتوحيد الخط إلى «تجوال») — بيئة نظيفة
        // بالكامل من الصفر (JDK OpenJDK 17.0.20 + Android SDK 34 جديد +
        // أدوات بناء 34.0.0 + تنزيل كل الاعتماديات عبر Gradle 8.5 و
        // AGP 8.2.2 وKotlin 1.9.22): البناء الفعلي الكامل `assembleMini`
        // نجح من أول محاولة دون أي خطأ ترجمة أو تحذير Kotlin واحد،
        // وتجاوز `lintVital` (المجهض على الخطأ) بلا ملاحظة، ونجح طقم
        // اختبارات الوحدة كاملًا (46 اختبارًا: خطوط المواد 11، مدخلات
        // المواقع 6، مدقق النزول الطارئ 7، تخطيط الشبكة 22)، والفحص
        // الشامل `lintMini` وجد صفر أخطاء و11 تحذيرًا معلوماتيًا مقصودًا
        // (عشرة اقتراحات ترقية اعتماديات لا تُنفَّذ عمدًا — سياسة
        // التثبيت والأوفلاين — وواحد داخلي في مكتبة فحص Compose نفسها
        // خارج سيطرة المشروع). فحص يدوي لسلامة تبديل الخط إلى تجوال:
        // لا مراجع معلّقة لـHeadingFont/BodyFont ولا لخط أريف رقعة في
        // الكود، ومصغّر الموارد حذف ملفات الخط القديم من الحزمة فعلًا
        // (تحقّق بمحتوى الـAPK: ملفات تجوال الثلاثة فقط). مراجعة أنماط
        // الحالة في شاشة الطوارئ المعاد كتابتها (مفاتيح القوائم، جمع
        // الحالة بدورة الحياة، حفظ الحقول عبر العملية) سليمة، ومسح
        // TODO/FIXME والدوال الميتة و@Deprecated و@Suppress نظيف.
        // ملاحظة موجبة للنشر: آخر نسخة مصغّرة منشورة في buled (2.12.0)
        // سبقت التزام الواجهة الأخير، فكان لا بد من بناء ونشر نسخة
        // تشمل الكود الحالي. رُفعت هوية الإصدار إلى 2.12.1 (رمز 26)
        // ووُثّقت النسخة المصغّرة `majarra-mini-2.12.1.apk` في مستودع
        // `buled`.
        // 2.12.2: جلسة التحقق العميق الثامنة من أخطاء البناء على الكود
        // الحالي (الالتزام ba86245، هوية 2.12.1) — بيئة نظيفة بالكامل من
        // الصفر (OpenJDK 17.0.20 جديد + Android SDK 34 جديد عبر أدوات سطر
        // الأمر + أدوات بناء 34.0.0 + تنزيل كل الاعتماديات عبر Gradle 8.5
        // وAGP 8.2.2 وKotlin 1.9.22): البناء الفعلي الكامل `assembleMini`
        // نجح من أول محاولة دون أي خطأ ترجمة أو تحذير Kotlin واحد، وتجاوز
        // `lintVital` (المجهض على الخطأ) بلا ملاحظة، ونجح طقم اختبارات
        // الوحدة كاملًا في الأنماط الثلاثة (46 اختبارًا × 3 أنماط = 138
        // تنفيذًا، 0 فشل: خطوط المواد 11، مدخلات المواقع 6، مدقق النزول
        // الطارئ 7، تخطيط الشبكة 22)، والفحص الشامل `lintMini` وجد صفر
        // أخطاء و11 تحذيرًا معلوماتيًا مقصودًا (عشرة اقتراحات ترقية
        // اعتماديات لا تُنفَّذ عمدًا — سياسة التثبيت والأوفلاين — وواحد
        // داخلي في مكتبة فحص Compose نفسها خارج سيطرة المشروع). الفحص
        // اليدوي العميق سطّرًا سطرًا شمل: مطابقة الترحيل 7→8 مع الكيانات
        // (جدول طلبات الاحتياج وأعمدة السحب الجديدة وملاحظات النزول)
        // حرفيًا، ومطابقة دورة الموافقة/الرفض/الاسترجاع لطلبات الاحتياج
        // ودورة السحب/قرار الإصلاح/الإرجاع مع نص تعليمات.md، ومسحًا آليًا
        // شاملًا للدوال الخاصة والداخلية (63 دالة — كلها مستعملة، لا دوال
        // ميتة)، ومسح TODO/FIXME و`@Deprecated` و`@Suppress` (نظيف)،
        // ومراجعة منطق اللمس في شاشة المجرة (إعادة تهيئة المراجع عند تغيّر
        // عدد الأصابع، قفل السحب أثناء انيميشن الترتيب، إطلاق مؤقّت الضغط
        // المطوّل من نطاق التركيب صراحة) وخطة الطوارئ في
        // `NetworkLayout.arrange` (لا ترمي استثناءً ولا تُعيد نقصًا أبدًا:
        // ذكي ← قوة-موجهة ← شبكة). النتيجة: **صفر أخطاء بناء تُصلَح في
        // الكود**. رُفعت هوية الإصدار إلى 2.12.2 (رمز 27) حتى تحمل
        // النسخة المصغّرة الجديدة هوية مستقلة عن المنشورة سابقًا (رمز
        // 26)، ووُثّقت النسخة المصغّرة `majarra-mini-2.12.2.apk` في
        // مستودع `buled`.
        // 2.13.0: جلسة تعديلات منطق واجهات التطبيق (المواد والإحصائيات)
        // حسب تعليمات الجلسة: (1) حقل «نوع المادة» عند الإضافة — «مادة
        // اتصال» تُعرَّف في الكتالوج الموحد وفي حوار إضافة مادة احتياج
        // الموقع (عمود type + ترحيل 8→9). (2) خيار «التبعيات» يظهر فقط
        // لمواد الاتصال بجانب «سحب للصيانة»، ويفتح واجهة تبعيات مخصصة
        // بزر (+) لإضافة الملحقات (واير، كواكسل، مايك) وبنفس منطق
        // المواد: لكل تبعية «سحب للصيانة» الخاص بها عبر دورة السحوبات
        // (عمود parentName يميز سحب التبعية). (3) إعادة هيكلة الإحصائيات
        // بالكامل: عدد المواقع النشطة + عدد المواد حاليًا (مواد وتبعيات
        // ناقص المسحوبات المفتوحة) + سجل آخر العمليات (سحب/إرجاع/نزول
        // طارئ/إضافة مادة/صيانة) بعناصر قابلة للنقر تنقل المستخدم إلى
        // واجهة الموقع الفرعية الخاصة بالعملية (وسيط section في مسار
        // site_details). وأُصلح أثناء الفحص: فلترة المواد الرئيسية صارت
        // تحصر السحب المفتوح بلا parentName فلا يخفي سحب تبعية مادة
        // رئيسية تحمل الاسم نفسه، وإعادة تسمية مادة تُرحِّل مراجع الأم
        // في التبعيات والسحوبات.
        // 2.13.3: جلسة التحقق العميق الحادية عشرة من أخطاء البناء —
        // بناء فعلي كامل من بيئة نظيفة تمامًا (OpenJDK 17.0.20 جديد +
        // Android SDK 34 وأدوات بناء 34.0.0 جديدان + تنزيل كل الاعتماديات
        // عبر Gradle 8.5 / AGP 8.2.2 / Kotlin 1.9.22): نجح `assembleMini`
        // من أول محاولة بصفر خطأ ترجمة وصفر تحذير، ونجح طقم اختبارات
        // الوحدة كاملًا (46 اختبارًا: خطوط المواد 11، مدخلات المواقع 6،
        // مدقق النزول الطارئ 7، تخطيط الشبكة 22)، وأعطى `lintMini` صفر
        // أخطاء و11 تحذيرًا معلوماتيًا مقصودًا (عشرة اقتراحات ترقية
        // اعتماديات لا تُنفَّذ عمدًا وواحد داخلي في مكتبة فحص Compose
        // نفسها)، وتحقّق `apksigner` و`aapt2` من سلامة التوقيع والبنية.
        // لم تُرصد أي أخطاء بناء تحتاج إصلاحًا هذه الجلسة؛ رُفعت هوية
        // الإصدار إلى 2.13.3 (رمز 31) ونُشرت النسخة المصغّرة
        // `majarra-mini-2.13.3.apk` في مستودع `buled`.
        // 2.13.2: جلسة التحقق العميق العاشرة من أخطاء البناء — البناء
        // الفعلي الكامل من بيئة نظيفة بالكامل (OpenJDK 17.0.20 جديد +
        // Android SDK 34 جديد عبر أدوات سطر الأمر + أدوات بناء 34.0.0 +
        // تنزيل الاعتماديات كاملة عبر Gradle 8.5 / AGP 8.2.2 / Kotlin
        // 1.9.22) كشف هذه المرة خمسة عشر خطأ ترجمة حقيقيًا أدخلها الرفع
        // الجماعي الأخير (الالتزام d04b7b7) في SitesScreen.kt وCommon.kt،
        // أُصلحت جميعها بدقة مع حفظ نية الكود المقصودة: (1-3) استيرادات
        // ناقصة: animateFloat على InfiniteTransition وdrawIntoCanvas
        // وnativeCanvas داخل نطاق الرسم. (4) تمرير إيزنغ الخطي في موضع
        // وضع التكرار داخل infiniteRepeatable — صار إيزنغًا للتوين بدوران
        // خطي متصل. (5) قراءة لون المادة من MaterialTheme داخل نطاق
        // drawBehind غير التركيبي — رُفعت القراءة خارج نطاق الرسم.
        // (6) استدعاء snapTo المعلّق مباشرة داخل نطاق الإيماءة المقيّد
        // التعليق — صار يُطلق من نطاق التركيب بقفزات متسلسلة على نفس
        // المُرسِل فلا تضيع دلتا ولا يهتز المؤشر. (7-8) استدعاء
        // AnimatedVisibility بمستقبل ColumnScope الضمني داخل عناصر قائمة
        // كسولة — سُمّيت الدالة العلوية كاملةً صراحةً. (9) وسائط مسماة
        // عند استدعاء قيمة من نوع دالة في حوار الإضافة — صارت موقعيه حسب
        // ترتيب التوقيع. (10) بطاقة Material3 القابلة للنقر تشترط منذ
        // 1.2.0 معاملاً من نوع MutableInteractionSource لا الواجهة العامة،
        // فوُفّق نوع معامل في بطاقة «مجرة» المشتركة معها. وأُصلح أيضًا
        // خطأ بناء جسيم في المستودع نفسه: ملف غلاف غرادل
        // (gradle-wrapper.jar) كان مرفوعًا تالفًا (محتواه الثنائي مشوّه
        // برموز الاستبدال النصية) فكان ./gradlew يفشل فورًا قبل أي ترجمة؛
        // أُعيد توليده سليمًا بأمر غلاف غرادل 8.5 الرسمي. إعادة البناء
        // الكامل (ترجمة Kotlin + KSP لـRoom وHilt + مترجم Compose + تصغير
        // R8 كامل + تقليص موارد + lintVital) نجحت بعدها بصفر خطأ وصفر
        // تحذير Kotlin واحد، ونجح طقم اختبارات الوحدة كاملًا عبر الأنماط
        // الثلاثة (46 اختبارًا × 3 أنماط = 138 تنفيذًا، 0 فشل). رُفعت
        // هوية الإصدار إلى 2.13.2 (رمز 30) ووثّقت النسخة المصغّرة
        // `majarra-mini-2.13.2.apk` في مستودع `buled`.
        // 2.13.1: جلسة التحقق العميق التاسعة من أخطاء البناء — على كود
        // 2.13.0 مع إصلاح تحديث الإحصائيات عند العودة (الالتزام 525eaee):
        // بناء فعلي كامل من بيئة نظيفة بالكامل أُعدّت من الصفر (OpenJDK
        // 17.0.20 + Android SDK 34 + أدوات بناء 34.0.0 وGradle 8.5 /
        // AGP 8.2.2 / Kotlin 1.9.22 مع تنزيل الاعتماديات كاملة) — نجح
        // `assembleMini` من أول محاولة بصفر خطأ ترجمة وصفر تحذير Kotlin،
        // وتجاوز `lintVital` (المجهض على الخطأ) بلا ملاحظة، ونجح طقم
        // اختبارات الوحدة كاملًا (46 اختبارًا: خطوط المواد 11، مدخلات
        // المواقع 6، مدقق النزول الطارئ 7، تخطيط الشبكة 22). الفحص
        // اليدوي العميق: مطابقة الترحيل 8→9 مع الكيانات حرفيًا (عمود
        // `type` بافتراض آمن، جدول `material_dependencies` بفهرسه الفريد
        // على (siteId, parentName, name)، وعمود `parentName` في
        // `withdrawals`)، ومحوّل `MaterialType` يعود إلى NORMAL بأمان لأي
        // قيمة مجهولة، وتحقق حالات استخدام التبعيات (اسم غير فارغ، حد
        // الطول 60، رسالة عربية للتكرار قبل الفهرس الفريد)، وربط Hilt
        // كامل (توفير DAO + @Binds للمستودع)، وربط الإحصائيات التفاعلية
        // (وسيط `section` بقيمته الافتراضية في المسار والتنقل من سجل
        // العمليات إلى القسم المناسب لكل نوع عملية)، وتحديث الإحصائيات
        // عند كل عودة فعلية عبر مراقب دورة الحياة (تسجيل في
        // DisposableEffect وإزالة عند التفكيك). مسح آلي شامل للدوال
        // الخاصة والداخلية غير المستعملة وTODO/FIXME و`@Deprecated`
        // و`@Suppress` نظيف تمامًا — لا أخطاء بناء تُصلَح في الكود.
        // رُفعت هوية الإصدار إلى 2.13.1 (رمز 29) ونُشرت النسخة
        // المصغّرة في مستودع `buled`.
        versionCode = 31
        versionName = "2.13.3"
        vectorDrawables { useSupportLibrary = true }
    }

    buildTypes {
        release {
            isMinifyEnabled = true
            isShrinkResources = true
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }

        // النسخة المصغّرة (جلسة التحقق من البناء 2.9.1): نسخة مضغوكة
        // قابلة للتثبيت مباشرة — نفس تصغير وتقليص الموارد في release
        // (R8 + تقليص الموارد) لكنها موقعة بمفتاح التصحيح، فينتج ملف
        // واحد صغير الحجم جاهز للتجربة الشخصية دون إعداد مفاتيح توقيع.
        create("mini") {
            initWith(getByName("release"))
            isMinifyEnabled = true
            isShrinkResources = true
            signingConfig = signingConfigs.getByName("debug")
            versionNameSuffix = "-mini"
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }

    buildFeatures { compose = true }
    composeOptions { kotlinCompilerExtensionVersion = "1.5.8" }

    packaging { resources.excludes += "/META-INF/{AL2.0,LGPL2.1}" }

    // اختبارات الوحدة: تفشل بدل أن تُرجع قيمًا وهمية صامتة
    testOptions {
        unitTests.isReturnDefaultValues = false
    }

    lint {
        abortOnError = true
    }
}

// اختبارات الوحدة JVM خالصة صغيرة (JUnit + coroutines-test) — كومة 192م
// تكفيها بسعة، فتمنع تضخيم عامل الاختبار (الافتراضي ~500م) فوق ذاكرة
// البناء على الأجهزة محدودة الذاكرة (نفس فلسفة 1536م أعلاه).
tasks.withType<Test>().configureEach {
    maxHeapSize = "192m"
}

dependencies {
    // الأساسيات
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.activity:activity-compose:1.8.2")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")
    implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.7.0")
    // مراقبة الـ Flow بأمان من دورة الحياة (بدل collectAsState غير المرتبط بالدورة)
    implementation("androidx.lifecycle:lifecycle-runtime-compose:2.7.0")

    // Jetpack Compose (Material 3)
    implementation(platform("androidx.compose:compose-bom:2024.02.01"))
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.ui:ui-graphics")
    implementation("androidx.compose.ui:ui-tooling-preview")
    implementation("androidx.compose.material3:material3")
    implementation("androidx.compose.material:material-icons-extended")
    implementation("androidx.compose.animation:animation")

    // التنقل
    implementation("androidx.navigation:navigation-compose:2.7.7")

    // Hilt — حقن الاعتماديات
    implementation("com.google.dagger:hilt-android:2.50")
    ksp("com.google.dagger:hilt-compiler:2.50")
    implementation("androidx.hilt:hilt-navigation-compose:1.1.0")

    // Room — قاعدة البيانات المحلية
    implementation("androidx.room:room-runtime:2.6.1")
    implementation("androidx.room:room-ktx:2.6.1")
    ksp("androidx.room:room-compiler:2.6.1")

    // الصور المحلية فقط (مرفقات) — لا يحتاج إنترنت
    implementation("io.coil-kt:coil-compose:2.5.0")

    debugImplementation("androidx.compose.ui:ui-tooling")

    // اختبارات الوحدة (التحقق من المدخلات)
    testImplementation("junit:junit:4.13.2")
    testImplementation("org.jetbrains.kotlinx:kotlinx-coroutines-test:1.7.3")
}
